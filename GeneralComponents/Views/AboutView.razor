@page "/aboutview"
@using System.Runtime.InteropServices

<h1>About</h1>

<h4>Version 2020-03-10</h4>

<h3>Currently executing on <strong>@runtime</strong>.</h3>

@functions {
    string runtime = RuntimeInformation.IsOSPlatform(OSPlatform.Create("WEBASSEMBLY"))
        ? "Mono WebAssembly"
        : ".NET Core";
}
